---
title: "Teste manual — chip de sync error"
scope: "file"
path: ["src/**/*.ts"]
severity_min: "medium"
enabled: true
---

@kody-sync

## Instructions
- The result object shape is defined in `docs/result-objects.md` (see also `lib/nonexistent-helper.ts`).
- Este arquivo cita referências que NÃO existem no repo de propósito: a rule deve importar normalmente E exibir o chip "reference sync error" com o detalhe de quais arquivos falharam — deixando claro que a rule continua ativa.

<!-- resync 195856 -->

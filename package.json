{
    "name": "tiny-url",
    "version": "0.1.0",
    "private": true,
    "description": "Minimal URL shortener — fixture project for Kodus E2E quality gates.",
    "type": "module",
    "scripts": {
        "dev": "tsx watch src/index.ts",
        "build": "tsc",
        "start": "node dist/index.js",
        "test": "vitest run",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "express": "^4.19.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^22.0.0",
        "supertest": "^7.0.0",
        "tsx": "^4.19.0",
        "typescript": "^5.6.0",
        "vitest": "^2.0.0"
    },
    "engines": {
        "node": ">=20"
    }
}
